# features

- add some kind of penalty system for wrong presses. maybe propagate fire faster?
  tried blocking input for about 1 second, but multiple timers is difficult.
- display error message to explain penalty?
- add score? game timer?
- display chars per minute, accuracy after game.
- display per-char latency after game?

- flag to control chars per minute.
- add flag to record key press timing history.
- add flag to read and display key press timing history.
- flag to practice only slowest letters? (this duplicates custom dictionaries a bit.)
- thinking i don't need a game menu. different modes (game, stats) will be accessible from flags.
- need a high score function.
- high score should scale with speed. and also reward diverse character sets?
- find a way to restore terminal config if user quits with ctrl-c?
- at game end (both win and loss), should show chars per minute, total errors.
- add a flag to set seed?
- use separate seed for words so that word list is deterministic?

# misc

good intro to terminal apps:
https://sahaj.dev/blog/tui-from-scratch
https://github.com/sahaj-b/go-attend

considering crossterm for terminal management.

need one flag to allow adding words, another to allow adding single chars.
the frequency of appearance in each should control frequency in the game.

2026-04-05
noted some interesting behavior with cargo build.
if you pass the --message-format short flag,
cargo remembers that and persists it for all build runs where the code does not recompile.

~/.cargo/bin/dist init
~/.cargo/bin/dist build

vi Cargo.toml
git tag v1.0.3
git push --tags
